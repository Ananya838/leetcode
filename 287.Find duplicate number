class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        hashset = set()
        for i in range(len(nums)):
            if nums[i] not in hashset:
                hashset.add(nums[i])
            else:
                return nums[i]
