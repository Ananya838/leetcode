class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        n=len(nums)
        hash_map={}
        for a in range(0,n):
            b=target-nums[a]
            if b in hash_map:
                return [a,hash_map[b]]
            hash_map[nums[a]]=a
        
