class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        n=len(nums)
        nl=Counter(nums)
        for key,value in nl.items():
            if value>n//2:
                break
        return key

     
